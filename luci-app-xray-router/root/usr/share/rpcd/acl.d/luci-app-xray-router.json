{
  "luci-app-xray-router": {
    "description": "Read Xray Router status, captured traffic metadata, and configuration including proxy credentials; manage the stack and captures with write access",
    "read": { "ubus": { "luci.xray-router": ["get", "status", "job", "diagnostics"], "luci.xray-inspector": ["options", "get"] } },
    "write": { "ubus": { "luci.xray-router": ["start"], "luci.xray-inspector": ["capture", "stop"] } }
  }
}
